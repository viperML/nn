{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            // "program": "${workspaceFolder}/build/nn",
            "program": "${command:cmake.launchTargetPath}",
            "args": [
                "${workspaceFolder}/data"
            ],
            "cwd": "${workspaceFolder}",
        }
    ]
}
